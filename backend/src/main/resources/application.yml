server:
  port: 8080

spring:
  application:
    name: campus-activity-backend
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: ${DB_URL:jdbc:sqlserver://localhost:57961;databaseName=campus_activity;encrypt=true;trustServerCertificate=true;integratedSecurity=true}
    username: ${DB_USERNAME:}
    password: ${DB_PASSWORD:}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    com.campus.activity: debug
